venv/
__pycache__/
*.pyc
# Full thesis methodology, spec, and implementation plan - kept locally for
# reference, never tracked (student ID, unpublished research design).
private/
# Binary, regenerable from a seed at any time - the JSON triples/entities
# files are the reviewable, diffable record.
*.gpickle
