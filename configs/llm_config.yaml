# Placeholder structure - values pending API key provisioning (reference doc §3.3, §8).

generation_model:
  # writes dataset documents, task phrasing (reference doc §3.3: Claude Haiku 4.5)
  provider: null
  model_id: null
  temperature: 0.7
  max_tokens: 2048

backbone_model:
  # drives the agent's ReAct loop, tool calls, validation checks
  # DO NOT downgrade this to save cost (reference doc §3.3, §6)
  provider: null
  model_id: null
  temperature: 0.0
  max_tokens: 4096

api:
  endpoint: null  # managed cloud endpoint: Bedrock / Vertex AI / Azure AI Foundry
  api_key_env_var: "MEMARCH_LLM_API_KEY"

budget:
  cap_eur: 350
  alert_threshold_eur: 300
